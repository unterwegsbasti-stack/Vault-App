# vault-app

Dieses Repository enthält ein Android Studio Projekt (Jetpack Compose) für die "vault"-App. Ich lege in mehreren kleineren Schritten die Projektdateien an. Dies ist der erste Commit mit dem Projekt-Gerüst.

Was als nächstes kommt (auf Wunsch):
- App-Modul mit Gradle-Konfiguration, MainActivity, Theme und deiner AddEntryScreen-Datei
- Ergänzung der Datentypen (VaultEntry, EntryType)
- GitHub Actions Workflow zum Bauen der debug-APK

Sobald du bereit bist, fahre ich mit dem App-Modul fort.

Build (wenn das Projekt komplett ist):
- ./gradlew assembleDebug -> app/build/outputs/apk/debug/app-debug.apk

